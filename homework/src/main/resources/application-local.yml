# 本地开发环境配置 - 完全禁用Nacos
spring:
  config:
    import: # 清空import配置
  cloud:
    nacos:
      config:
        enabled: false
        import-check:
          enabled: false
      discovery:
        enabled: false

# 本地开发服务器配置
server:
  port: 18080

# 本地开发日志配置
logging:
  level:
    com.jlm.homework: DEBUG
